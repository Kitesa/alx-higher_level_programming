#!/usr/bin/python3

def multiple_returns(sentence):
    tupl_a = ();
    count = 0
    for i in sentence:
        count += 1
    list_a = list(tuple_a)
    list_a[0] = count
    list_a[1] = sentence[0]

    return(tuple(list_a))
